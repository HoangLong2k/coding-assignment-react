import Loading, { toggleLoading } from "./loading";
import { default as Button } from "./button";
import Modal, { IModalRef } from "./modal";
export { Loading, toggleLoading, Button, Modal, IModalRef };
